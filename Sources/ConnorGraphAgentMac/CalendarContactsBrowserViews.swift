import SwiftUI
import ConnorGraphCore
import ConnorGraphAppSupport

// Calendar/Contacts pane implementations live in AppListDetailPanes.swift so Xcode/SourceKit
// always resolves the switch references in the same compilation unit.
